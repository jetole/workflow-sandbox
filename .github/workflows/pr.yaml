name: Run on PR's

on:
  pull_request:
  push:

jobs:
  init:
    name: Init steps
    runs-on: ubuntu-latest

    steps:
      - name: Show env
        if: ${{ env.GITHUB_EVENT_NAME == "pull_request" }}
        run: env | sort
