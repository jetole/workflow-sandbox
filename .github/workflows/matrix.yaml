name: Eval matrix

on:
  workflow_dispatch:

jobs:
  eval:
    name: Evaluate
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - datacenter: ca
          - datacenter: us

    env:
      test_var: ${{ matrix.datacenter }}

    steps:
      - name: Env
        run: env | sort
